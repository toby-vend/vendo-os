---
phase: 01-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/lib/queries.ts
  - scripts/utils/db.ts
autonomous: true
requirements: [INFR-02]
must_haves:
  truths:
    - "skills table exists in the database after schema init"
    - "skills_fts FTS5 virtual table exists in Turso/libsql after schema init"
    - "brand_hub table exists with client_id index"
    - "drive_watch_channels table exists with unique channel_id"
    - "task_runs table exists with status, client, and created_at indexes"
    - "Existing tables and data are unaffected by the migration"
  artifacts:
    - path: "web/lib/queries.ts"
      provides: "initAuthSchema extended with 5 new tables + indexes"
      contains: "CREATE TABLE IF NOT EXISTS skills"
    - path: "scripts/utils/db.ts"
      provides: "Local sql.js schema extended with new tables (no FTS5)"
      contains: "CREATE TABLE IF NOT EXISTS skills"
  key_links:
    - from: "web/lib/queries.ts"
      to: "Turso database"
      via: "initAuthSchema CREATE TABLE IF NOT EXISTS"
      pattern: "skills|brand_hub|drive_watch_channels|task_runs"
    - from: "scripts/utils/db.ts"
      to: "Local SQLite"
      via: "initSchema CREATE TABLE IF NOT EXISTS"
      pattern: "skills|brand_hub|drive_watch_channels|task_runs"
---

<objective>
Extend the database schema with the 5 new tables required by the skills layer: skills, skills_fts, brand_hub, drive_watch_channels, and task_runs.

Purpose: Phase 2+ needs these tables to exist. Adding them now (additive only, IF NOT EXISTS) is zero-risk and unblocks all downstream work.
Output: Both schema paths (libsql/Turso production + sql.js local scripts) updated with new DDL.
</objective>

<execution_context>
@/Users/Toby_1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Toby_1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md

@web/lib/queries.ts
@scripts/utils/db.ts

<interfaces>
<!-- Current initAuthSchema in queries.ts (around line 650): -->
From web/lib/queries.ts:
```typescript
export async function initAuthSchema(): Promise<void> {
  // Creates users, channels, user_channels, channel_permissions, sessions, user_oauth_tokens tables
  // Uses client.executeMultiple with CREATE TABLE IF NOT EXISTS statements
}
```

<!-- Current initSchema in scripts/utils/db.ts: -->
From scripts/utils/db.ts:
```typescript
// Uses sql.js (NOT libsql) — FTS4 only, no FTS5
// CREATE TABLE IF NOT EXISTS pattern throughout
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new table DDL to libsql schema init (Turso production path)</name>
  <files>web/lib/queries.ts</files>
  <action>
    Locate `initAuthSchema()` in `web/lib/queries.ts` (around line 650). Append the following DDL statements after the existing table creation block, inside the same `executeMultiple` call (or as a second `executeMultiple` call if cleaner):

    Use the exact DDL from 01-RESEARCH.md "New Table DDL (libsql path)" section:

    1. `CREATE TABLE IF NOT EXISTS skills` — with columns: id INTEGER PRIMARY KEY, drive_file_id TEXT NOT NULL UNIQUE, title, content, content_hash, channel, skill_type, drive_modified_at, indexed_at, version INTEGER NOT NULL DEFAULT 1
    2. `CREATE VIRTUAL TABLE IF NOT EXISTS skills_fts USING fts5(title, content, content='skills', tokenize='unicode61')` — FTS5 is available in Turso/libsql
    3. `CREATE TABLE IF NOT EXISTS brand_hub` — with columns per CONTEXT.md decisions + `CREATE INDEX IF NOT EXISTS idx_brand_hub_client ON brand_hub(client_id)`
    4. `CREATE TABLE IF NOT EXISTS drive_watch_channels` — with channel_id TEXT NOT NULL UNIQUE
    5. `CREATE TABLE IF NOT EXISTS task_runs` — with all columns per CONTEXT.md + three indexes (client_id, status, created_at)

    All statements use `IF NOT EXISTS` — safe to run repeatedly. Do NOT modify any existing table DDL.

    Rename the function from `initAuthSchema` to `initSchema` to reflect it now covers more than auth. Update the single call site (search for `initAuthSchema` in the codebase) and keep `initAuthSchema` as a deprecated alias export for safety.
  </action>
  <verify>
    <automated>cd /Users/Toby_1/Vendo-OS && npx tsx -e "import { initAuthSchema } from './web/lib/queries.js'; console.log('initAuthSchema import OK')" && npx tsx -e "import { initSchema } from './web/lib/queries.js'; console.log('initSchema import OK')"</automated>
  </verify>
  <done>initAuthSchema/initSchema in queries.ts contains DDL for all 5 new tables + indexes + FTS5 virtual table. Both export names resolve. Existing tables untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add new table DDL to sql.js schema init (local scripts path)</name>
  <files>scripts/utils/db.ts</files>
  <action>
    Locate the schema init section in `scripts/utils/db.ts`. Append `CREATE TABLE IF NOT EXISTS` statements for: skills (same columns, no FTS5), brand_hub, drive_watch_channels, task_runs.

    **Do NOT add `skills_fts`** — sql.js 1.11.0 only supports FTS4, and the skills FTS table is only queried by the web app (which uses Turso/libsql). Add a comment: `-- skills_fts omitted: FTS5 not available in sql.js; queried via web app (Turso) only`

    Add the same indexes as the libsql path (idx_brand_hub_client, idx_task_runs_client, idx_task_runs_status, idx_task_runs_created).

    All statements use `IF NOT EXISTS`. Do NOT modify existing table DDL.
  </action>
  <verify>
    <automated>cd /Users/Toby_1/Vendo-OS && npx tsx scripts/utils/db.ts 2>&1 | head -5 && echo "db.ts executed without error"</automated>
  </verify>
  <done>scripts/utils/db.ts contains DDL for skills, brand_hub, drive_watch_channels, task_runs (no FTS5). Script runs without error. Existing tables untouched.</done>
</task>

</tasks>

<verification>
1. Import check: `npx tsx -e "import { initSchema } from './web/lib/queries.js'"` — no errors
2. Local script: `npx tsx scripts/utils/db.ts` — runs without errors
3. `npx tsc --noEmit` — no type errors
</verification>

<success_criteria>
- All 5 new tables defined in Turso/libsql path (including FTS5 virtual table)
- 4 new tables defined in sql.js path (FTS5 intentionally omitted with comment)
- All indexes created
- Existing tables and schema completely untouched
- Both schema init paths run without error
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-02-SUMMARY.md`
</output>
