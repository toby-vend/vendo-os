---
phase: 13-offline-caching
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/server.ts
  - public/offline.html
  - public/offline-partial.html
autonomous: true
requirements: [OFFL-04, OFFL-05]

must_haves:
  truths:
    - "Every Fastify response includes a Vary: HX-Request header"
    - "A branded offline fallback page exists at /offline.html matching the dark theme"
    - "A minimal HTML snippet exists at /offline-partial.html suitable for HTMX swap targets"
  artifacts:
    - path: "web/server.ts"
      provides: "Vary: HX-Request header on all responses"
      contains: "Vary.*HX-Request"
    - path: "public/offline.html"
      provides: "Full-page offline fallback"
      min_lines: 20
    - path: "public/offline-partial.html"
      provides: "Inline offline snippet for HTMX swap targets"
      min_lines: 3
  key_links:
    - from: "web/server.ts"
      to: "HTTP responses"
      via: "onSend hook adds Vary header"
      pattern: "reply\\.header.*Vary.*HX-Request"
---

<objective>
Add the Vary: HX-Request response header to all Fastify routes and create the two offline fallback HTML files that the service worker (Plan 02) will precache and serve.

Purpose: The Vary header enables correct HTTP cache keying when the same URL returns different responses for full-page vs HTMX partial requests. The offline files provide graceful degradation when network is unavailable.

Output: Modified server.ts with Vary header, new offline.html (branded full page), new offline-partial.html (minimal inline snippet).
</objective>

<execution_context>
@/Users/Toby_1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Toby_1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-offline-caching/13-CONTEXT.md
@.planning/phases/13-offline-caching/13-RESEARCH.md
@.planning/phases/12-pwa-foundation/12-01-SUMMARY.md

<interfaces>
<!-- Existing onSend hook in web/server.ts (line 228) where Vary header must be added -->
From web/server.ts line 228:
```typescript
app.addHook('onSend', async (_request, reply) => {
  reply.header('X-Content-Type-Options', 'nosniff');
  reply.header('X-Frame-Options', 'DENY');
  reply.header('Referrer-Policy', 'strict-origin-when-cross-origin');
  reply.header('Permissions-Policy', 'camera=(), microphone=(), geolocation=()');
  // Vary: HX-Request goes here
});
```

From public/sw.js (Phase 12 state):
```javascript
importScripts('https://storage.googleapis.com/workbox-cdn/releases/7.4.0/workbox-sw.js');
workbox.core.skipWaiting();
workbox.core.clientsClaim();
// Static asset CacheFirst route exists
// Phase 13 adds NetworkFirst routes + precaching in Plan 02
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Vary: HX-Request header and create offline fallback files</name>
  <files>web/server.ts, public/offline.html, public/offline-partial.html</files>
  <action>
**1. Vary header in web/server.ts (OFFL-05):**

Add `reply.header('Vary', 'HX-Request');` to the existing `onSend` hook at line 228, after the existing security headers. This is a single line addition inside the existing hook block — do not create a new hook.

**2. Branded offline fallback page — public/offline.html (OFFL-04):**

Create a standalone HTML page matching the existing VendoOS dark theme. Requirements:
- Full `<!DOCTYPE html>` document with viewport meta tag
- Reference `/assets/style.css` for base dark theme styles (this file is cached by the Phase 12 CacheFirst static route, so it will be available offline)
- Do NOT load Google Fonts or any external resources — use system font stack as fallback: `'Manrope', system-ui, -apple-system, sans-serif`
- Include the VendoOS logo (the green "V" SVG from the existing icons — inline it as SVG, do not reference an external file)
- Display "You're offline" as the primary heading
- Display a brief message: "Check your connection and try again"
- Include a retry button that calls `window.location.reload()`
- Style: dark background (#0A0A0A or matching --vendo-bg), muted text (#94A3B8), green accent (#22C55E) on the button, centred vertically and horizontally
- Keep it minimal — no nav, no sidebar, just the logo + message + button

**3. HTMX partial offline snippet — public/offline-partial.html (OFFL-04):**

Create a minimal HTML fragment (NO doctype, NO html/head/body tags) designed to render cleanly inside any HTMX swap target:
- A single `<div>` with inline styles (since it may render in contexts where CSS classes are not available)
- Flexbox row layout with a small wifi-off SVG icon (16x16) and text "You're offline"
- Use muted colour (#94A3B8), font-size 13px, Manrope font family
- Add `role="status"` and `aria-live="polite"` for accessibility
- Keep total height minimal — this is an inline notice, not a page replacement
  </action>
  <verify>
    <automated>grep -c "Vary.*HX-Request" web/server.ts && test -f public/offline.html && test -f public/offline-partial.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- web/server.ts onSend hook includes `Vary: HX-Request` header
- public/offline.html exists as a complete branded offline page with dark theme, VendoOS logo, message, and retry button
- public/offline-partial.html exists as a minimal HTML fragment suitable for HTMX swap injection
- Neither offline file loads external resources (Google Fonts, external CDNs)
  </done>
</task>

</tasks>

<verification>
- `grep -i 'vary.*hx-request' web/server.ts` returns the header line
- `public/offline.html` contains `<!DOCTYPE html>` and "offline" text
- `public/offline-partial.html` does NOT contain `<!DOCTYPE html>` or `<html>` tags
- `public/offline-partial.html` contains `role="status"`
- TypeScript compiles: `npx tsc --noEmit` (or project build command)
</verification>

<success_criteria>
1. Every HTTP response from Fastify includes `Vary: HX-Request` header
2. A branded offline.html page exists that renders correctly with only cached CSS
3. An offline-partial.html snippet exists that can be safely injected into any HTMX swap target
</success_criteria>

<output>
After completion, create `.planning/phases/13-offline-caching/13-01-SUMMARY.md`
</output>
