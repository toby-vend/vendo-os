---
phase: 11-responsive-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/views/layouts/base.eta
  - public/assets/style.css
autonomous: false
requirements:
  - RESP-01
  - RESP-02
  - RESP-03
  - RESP-04
  - RESP-05

must_haves:
  truths:
    - "On a screen below 768px, the sidebar is completely hidden — not collapsed, not off-screen, display:none"
    - "A fixed bottom tab bar appears on mobile with 4 tabs: Dashboard, Tasks, Clients, More"
    - "Each tab item meets the 48px minimum touch target"
    - "Tapping More opens a full-screen overlay listing all nav groups from sidebarConfig"
    - "The active tab is highlighted with Vendo green"
    - "No horizontal scrollbar appears on any page at 375px width"
    - "The viewport meta tag includes viewport-fit=cover"
    - "Full-height containers use dvh with vh fallback"
    - "Body overflow is auto on mobile so main-content scrolls"
  artifacts:
    - path: "web/views/layouts/base.eta"
      provides: "Tab bar HTML, More overlay HTML, updated JS"
      contains: "tab-bar"
    - path: "public/assets/style.css"
      provides: "Mobile tab bar CSS, More overlay CSS, global mobile reset"
      contains: ".tab-bar"
  key_links:
    - from: "web/views/layouts/base.eta"
      to: "public/assets/style.css"
      via: "CSS classes tab-bar, more-nav-overlay, tab-item"
      pattern: "tab-bar|more-nav-overlay"
    - from: "web/views/layouts/base.eta"
      to: "sidebarConfig"
      via: "Eta loop rendering More overlay items from it.sidebarConfig"
      pattern: "sidebarConfig.*forEach"
---

<objective>
Add the global mobile viewport foundation, bottom tab bar, and More navigation overlay to VendoOS.

Purpose: This is the layout foundation for all mobile work. The tab bar replaces the buggy sidebar on mobile and provides thumb-reachable navigation. The viewport/CSS reset prevents horizontal overflow and iOS Safari quirks.

Output: Updated base.eta with tab bar + More overlay markup and JS, updated style.css with mobile reset and tab bar/overlay CSS.
</objective>

<execution_context>
@/Users/Toby_1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Toby_1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-responsive-layout/11-CONTEXT.md
@.planning/phases/11-responsive-layout/11-RESEARCH.md

@web/views/layouts/base.eta
@public/assets/style.css
@web/lib/queries/sidebar.ts

<interfaces>
<!-- Key types the executor needs from the existing codebase -->

From web/lib/queries/sidebar.ts:
```typescript
export interface SidebarItem {
  id: string;
  label: string;
  href: string;
  permission?: string;
  hidden?: boolean;
}

export interface SidebarGroup {
  id: string;
  label: string;
  icon?: string;
  permission?: string;
  adminOnly?: boolean;
  items: SidebarItem[];
}

export type SidebarConfig = SidebarGroup[];
```

The `it.sidebarConfig` variable is available in base.eta and contains the full SidebarConfig array. The `it.currentPath` variable contains the current route path. The `it.user` variable contains the logged-in user object with `role` and `allowedRoutes`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global mobile viewport reset and tab bar + More overlay CSS</name>
  <files>public/assets/style.css, web/views/layouts/base.eta</files>
  <action>
**CSS changes (append to end of public/assets/style.css, inside a new clearly-commented section):**

1. Add a `/* === Phase 11: Mobile Responsive === */` comment section at the end of style.css.

2. **Desktop-only utility:** `.mobile-only { display: none; }` (cards shown only on mobile).

3. **Inside `@media (max-width: 768px)` block** (create a NEW one at the end — do not modify the existing one at ~line 940):

   a. **Viewport/overflow fix:** `body { overflow: auto; }` to override the existing `overflow: hidden` that breaks mobile scroll.

   b. **Hide sidebar entirely:** `.sidebar { display: none !important; }` and `.sidebar-backdrop { display: none !important; }` and `.mobile-menu-btn { display: none !important; }` — the tab bar replaces all of these.

   c. **dvh with fallback:** `.app-layout { height: 100vh; height: 100dvh; }` — browsers that don't support dvh use the vh fallback.

   d. **Desktop-only / mobile-only toggles:** `.desktop-only { display: none !important; }` and `.mobile-only { display: block; }`.

   e. **Tab bar CSS** per the research Pattern 1:
      - `.tab-bar`: `display: flex; position: fixed; bottom: 0; left: 0; right: 0; height: calc(56px + env(safe-area-inset-bottom)); padding-bottom: env(safe-area-inset-bottom); background: var(--glass-bg-strong, rgba(14,14,14,0.95)); backdrop-filter: saturate(180%) var(--glass-blur); -webkit-backdrop-filter: saturate(180%) var(--glass-blur); border-top: 1px solid var(--glass-border); z-index: 100; justify-content: space-around; align-items: stretch;`
      - `.tab-item`: `flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 3px; min-height: 48px; min-width: 48px; color: var(--vendo-text-muted); text-decoration: none; font-size: 10px; font-weight: 500; background: none; border: none; cursor: pointer; font-family: var(--vendo-font);`
      - `.tab-item.active, .tab-item:active`: `color: var(--vendo-green);`
      - `.tab-icon`: `width: 22px; height: 22px;`
      - `.tab-label`: `line-height: 1;`

   f. **Main content bottom padding** to clear the tab bar: `.main-content { padding-bottom: calc(56px + env(safe-area-inset-bottom) + 1rem); }`

   g. **More overlay CSS** per research Pattern 2:
      - `.more-nav-overlay`: `display: none; position: fixed; inset: 0; background: var(--vendo-bg, #0B0B0B); z-index: 300; flex-direction: column; overflow-y: auto;`
      - `.more-nav-overlay.open`: `display: flex;`
      - `.more-nav-header`: `display: flex; align-items: center; justify-content: space-between; padding: 1rem 1.25rem; border-bottom: 1px solid var(--glass-border); min-height: 56px; flex-shrink: 0;`
      - `.more-nav-header span`: `font-size: 17px; font-weight: 600; color: var(--vendo-text);`
      - `.more-nav-close`: `background: none; border: none; color: var(--vendo-text-muted); cursor: pointer; padding: 8px; min-width: 48px; min-height: 48px; display: flex; align-items: center; justify-content: center;`
      - `.more-nav-body`: `flex: 1; padding: 0.75rem 0; overflow-y: auto; -webkit-overflow-scrolling: touch;`
      - `.more-nav-group-label`: `padding: 0.5rem 1.25rem; font-size: 11px; font-weight: 600; color: var(--vendo-text-muted); text-transform: uppercase; letter-spacing: 0.5px;`
      - `.more-nav-item`: `display: flex; align-items: center; gap: 0.875rem; padding: 0 1.25rem; min-height: 48px; color: var(--vendo-text); text-decoration: none; font-size: 15px;`
      - `.more-nav-item:active`: `background: rgba(255,255,255,0.04);`
      - `.more-nav-item .nav-icon`: `width: 20px; height: 20px; flex-shrink: 0;`

   h. **Input font-size fix:** `input, select, textarea { font-size: 16px !important; }` to prevent iOS Safari auto-zoom.

   i. **Touch target minimum on interactive elements:** `button, .btn, select { min-height: 48px; }` (tab items already have this from tab-item rule).

4. **On desktop, hide the tab bar:** `.tab-bar { display: none; }` (outside the media query, at the top of the new section).

**base.eta viewport meta change (line 5):**
Change `<meta name="viewport" content="width=device-width, initial-scale=1.0">` to `<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">`. This is required for `env(safe-area-inset-bottom)` to work.

**base.eta tab bar HTML (insert after the closing `</div>` of `.app-layout` on line 149, before the `<script>` block):**

Add the tab bar nav element with 4 items. Use inline SVG icons (simple, standard icons — home/grid for Dashboard, clipboard/check for Tasks, users for Clients, ellipsis/more for More). Use `it.currentPath` for active state detection:
- Dashboard: active when `it.currentPath === '/'`
- Tasks: active when `it.currentPath` starts with `/tasks`
- Clients: active when `it.currentPath` starts with `/clients`
- More: never auto-active (it's a button, not a link)

The More tab must be a `<button>` (not an anchor) with `onclick="openMoreNav()"`.

**base.eta More overlay HTML (insert after the tab bar):**

A `<div class="more-nav-overlay" id="more-nav-overlay">` containing:
- A header with "Menu" text and a close button (X icon SVG) calling `closeMoreNav()`
- A body that loops through `it.sidebarConfig` with the same visibility logic as the sidebar (check `adminOnly`, `hidden`, `permission` using the same `canSee` helper already defined at line 55)
- Each group renders as a `.more-nav-group-label` followed by `.more-nav-item` anchors for each visible item
- Each item shows the group icon (if present) and the item label

**base.eta JS changes (inside the existing `<script>` block):**

1. Add `openMoreNav()` and `closeMoreNav()` functions per research Pattern 2.
2. Add `window.addEventListener('pageshow', closeMoreNav)` to handle bfcache (research Pitfall 7).
3. Guard the existing `toggleSidebar` function: add `if (window.innerWidth <= 768) return;` at the top so it does nothing on mobile.
  </action>
  <verify>
    <automated>cd /Users/Toby_1/Vendo-OS && node -e "
      const fs = require('fs');
      const css = fs.readFileSync('public/assets/style.css', 'utf8');
      const eta = fs.readFileSync('web/views/layouts/base.eta', 'utf8');
      const checks = [
        ['CSS has tab-bar', css.includes('.tab-bar')],
        ['CSS has more-nav-overlay', css.includes('.more-nav-overlay')],
        ['CSS has mobile-only', css.includes('.mobile-only')],
        ['CSS has dvh', css.includes('100dvh')],
        ['CSS has safe-area', css.includes('safe-area-inset-bottom')],
        ['CSS has 48px min', css.includes('min-height: 48px')],
        ['CSS has input 16px', css.includes('font-size: 16px')],
        ['ETA has viewport-fit=cover', eta.includes('viewport-fit=cover')],
        ['ETA has tab-bar', eta.includes('tab-bar')],
        ['ETA has more-nav-overlay', eta.includes('more-nav-overlay')],
        ['ETA has openMoreNav', eta.includes('openMoreNav')],
        ['ETA has closeMoreNav', eta.includes('closeMoreNav')],
        ['ETA has pageshow listener', eta.includes('pageshow')],
      ];
      let pass = true;
      checks.forEach(([name, ok]) => { console.log(ok ? 'PASS' : 'FAIL', name); if (!ok) pass = false; });
      process.exit(pass ? 0 : 1);
    "
    </automated>
  </verify>
  <done>
    - viewport-fit=cover in meta tag
    - Tab bar with 4 items (Dashboard, Tasks, Clients, More) rendered in base.eta
    - Tab bar visible on mobile (below 768px), hidden on desktop
    - Sidebar + backdrop + hamburger hidden on mobile via display:none!important
    - More overlay opens full-screen from sidebarConfig, closes via button or pageshow
    - dvh fallback, safe-area-inset-bottom padding, 16px input font-size, 48px touch targets all in CSS
    - Body overflow: auto on mobile so content scrolls
    - No horizontal scrollbar at 375px
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify tab bar, More overlay, and global mobile layout</name>
  <files>web/views/layouts/base.eta, public/assets/style.css</files>
  <action>
Human verification of the mobile layout changes. Open VendoOS in Chrome DevTools responsive mode at 375px and verify all items below.

What was built: Bottom tab bar replacing sidebar on mobile, full-screen More overlay, viewport/overflow fixes, safe area insets, and 48px touch targets.
  </action>
  <verify>
    1. Open VendoOS in Chrome DevTools responsive mode at 375px width
    2. Confirm the sidebar is completely gone on mobile — no hamburger button either
    3. Confirm a bottom tab bar is visible with 4 tabs: Dashboard, Tasks, Clients, More
    4. Tap each tab — Dashboard goes to /, Tasks goes to /tasks, Clients goes to /clients
    5. Confirm the active tab is highlighted in Vendo green
    6. Tap "More" — a full-screen overlay should appear with all navigation groups
    7. Confirm the More overlay has a close (X) button and lists all nav sections
    8. Tap a link in the More overlay — confirm it navigates correctly
    9. Scroll the main content on any page — confirm it scrolls naturally, no horizontal scrollbar
    10. Inspect any input/select on /tasks/new — confirm font-size is at least 16px
    11. On desktop (resize above 768px) — confirm the sidebar reappears and the tab bar is hidden
  </verify>
  <done>User confirms tab bar, More overlay, and mobile layout work correctly at 375px and 768px+ breakpoints</done>
</task>

</tasks>

<verification>
- No horizontal scrollbar at 375px on Dashboard, Tasks, Clients pages
- Tab bar visible on mobile with correct navigation
- More overlay shows all sidebar nav groups
- Sidebar completely hidden on mobile
- Touch targets meet 48px minimum
</verification>

<success_criteria>
- Tab bar appears on screens below 768px with 4 tabs navigating correctly
- More overlay opens full-screen showing all nav groups from sidebarConfig
- Sidebar, backdrop, and hamburger are display:none on mobile
- No horizontal scroll on any page at 375px
- viewport-fit=cover set, dvh units used, safe-area-inset-bottom applied
</success_criteria>

<output>
After completion, create `.planning/phases/11-responsive-layout/11-01-SUMMARY.md`
</output>
