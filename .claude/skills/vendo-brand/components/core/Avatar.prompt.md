Circular avatar — photo when `src` is set, otherwise tidy initials on a sage tile.

```jsx
<Avatar name="Max Rivens" src="/team/max.jpg" />
<Avatar name="Toby Raeburn" size={32} ring />
```

- Falls back to two-letter initials in mint. `ring` adds a mint status ring.
